import 'package:flutter/material.dart';

class NavBarWidgets extends StatelessWidget {
  final String title;
  const NavBarWidgets(this.title, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
